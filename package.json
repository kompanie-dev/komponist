{
  "name": "@kompanie/komponist",
  "version": "4.0.0",
  "license": "MIT",
  "repository": "github:kompanie-dev/komponist",
  "files": [
    "/source",
    "/index.css"
  ],
  "scripts": {
    "build": "esbuild --bundle index.css --outfile=build/komponist.css",
    "build-minified": "esbuild --bundle  --minify index.css --outfile=build/komponist.minified.css",
    "start": "npx diener --port=8000 --livereload"
  },
  "devDependencies": {
    "@kompanie/diener": "^1.0.4",
    "esbuild": "^0.25.5"
  }
}
