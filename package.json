{
  "name": "@kompanie/komponist",
  "version": "4.0.0",
  "license": "MIT",
  "repository": "github:kompanie-dev/komponist",
  "files": [
    "/source",
    "/index.css"
  ],
  "scripts": {
    "build": "npx parcel build index.css --dist-dir build --no-cache",
    "start": "npx diener --port=8000 --livereload"
  },
  "devDependencies": {
    "@kompanie/diener": "^1.0.4"
  }
}
